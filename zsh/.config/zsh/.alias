# Listing colours
alias ls='ls --color'

# Listing shorhand
alias ll='ls -hl'
alias la='ls -Ahl'

# Attach to default tmux session
alias t='tmux new-session -A -s default'

# Ranger
alias r='ranger'

# Use nvim by default
alias vi='nvim'
alias vim='nvim'
